# ============================================================
# Smart Coaching — Spring Boot Application Properties
# ============================================================

# ----- Server -----
server.port=${PORT:8080}

# ----- MySQL Database (Clever Cloud) -----
spring.datasource.url=jdbc:mysql://by7irqj6mq5vx7f5ea6o-mysql.services.clever-cloud.com:3306/by7irqj6mq5vx7f5ea6o?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=unmutzjr2imrydzb
spring.datasource.password=twNtBL57Pbb83GN5AK8q
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ----- JPA / Hibernate -----
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# ----- JWT -----
jwt.secret=SmartCoachingDemoSecretKey2024PleaseChangeThisToSomethingSecure
jwt.expiration-ms=86400000

# ----- CORS (allow frontend dev) -----
spring.web.cors.allowed-origins=*

# ----- Logging -----
logging.level.com.smartcoaching=INFO
